package com.icreservas.backend.controller;
/*
El Portero del Login
 Este es el controlador REST, donde el frontend envía las credenciales para iniciar sesión.

¿Qué hace?
Recibe una petición POST /login con email y contraseña.

Usa el AuthenticationManager para verificar las credenciales.

Si son válidas, genera un token JWT con JwtUtil.

Devuelve ese token al frontend.

Piensa en él como:
El recepcionista: revisa tus credenciales, y si son válidas, te da un pase VIP (el token) para que puedas moverte por todo el edificio (API).
 */

public class AuthController {

}

package com.icreservas.backend.repository;

/*
 * 
 El Buscador de Usuarios
 Este es tu Data Access Object (DAO) para usuarios.

¿Qué hace?
Te permite buscar usuarios por email (findByEmail), guardar nuevos usuarios, etc.

Hereda de JpaRepository, lo que le da muchas funciones listas para usar.

Piensa en él como:
El detective privado que busca usuarios en la base de datos por email, ID, etc.
 */
public record UsuarioRepository() {

}
